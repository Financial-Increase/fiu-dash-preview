// Canonical production origin. Use this (not window.location.origin) for any
// link meant to be shared or emailed — window.location.origin points at
// whatever environment generated it (localhost, a Lovable preview, a Vercel
// preview deploy), which breaks the link for the recipient.
export const APP_URL = "https://team.financialincrease.com";
